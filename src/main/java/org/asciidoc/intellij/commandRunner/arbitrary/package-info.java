/**
 * {@link org.asciidoc.intellij.commandRunner.arbitrary.AsciiDocRunnerArbitrary}
 * is the base-class to run commands / scripts in arbitrary languages.<br>
 * They are run in the background, so even long-running scripts don't block the UI.
 */
package org.asciidoc.intellij.commandRunner.arbitrary;
